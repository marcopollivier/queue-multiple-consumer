#http://docs.spring.io/spring-cloud-stream/docs/Brooklyn.SR3/reference/htmlsingle/#_rabbit_producer_properties

spring:
  application:
    name: producer-magneto
  rabbitmq:
    addresses: localhost
    virtual_host: xmen

  cloud:
    stream.bindings:

      #Producer
      FilaUnicaOutput:
        destination: FilaUnica
        producer:
          requiredGroups: Colossus


#    rabbit.bindings:
#
#        #Producer
#        FilaUnicaOutput:
#          produces:
#            bindingRoutingKey: 'xpto'